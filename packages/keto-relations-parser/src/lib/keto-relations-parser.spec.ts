import { ketoRelationsParser } from './keto-relations-parser';

describe('ketoRelationsParser', () => {
  it('should work', () => {
    expect(ketoRelationsParser()).toEqual('keto-relations-parser');
  });
});
